<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('partials/head') %>
</head>
<body class="centered-layout fade-in">

<%- include('partials/toasts') %>

<div class="dashboard-card">
    <div class="card-header-image">
        <img src="/images/dashboard-welcome-header.jpg" alt="Allcastle Homes Logo">
    </div>

    <h1>Welcome!</h1>
    <p class="lead">You are successfully checked in.</p>

    <% if (locals.user) { %>
        <p class="mb-4 text-white">Hello, <strong><%= locals.user.email; %></strong>!</p>
        <a href="/logout" class="btn-custom btn-danger-custom">Checkout / Logout</a>
    <% } else { %>
        <a href="/registration" class="btn-custom btn-primary-custom">Back to Registration</a>
    <% } %>
</div>

<%- include('partials/scripts') %>
</body>
</html>
