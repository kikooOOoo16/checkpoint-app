{
  "$id": "VisitorRegistrationSchema",
  "title": "Visitor Registration Schema",
  "type": "object",
  "additionalProperties": false,
  "required": ["name", "email", "company", "purpose"],

  "properties": {
    "name": {
      "type": "string",
      "minLength": 2,
      "maxLength": 100,
      "pattern": "^[A-Za-zÀ-ž'\\-\\s]+$",
      "errorMessage": {
        "type": "Name must be a string.",
        "minLength": "Name must contain at least 2 characters.",
        "maxLength": "Name must not exceed 100 characters.",
        "pattern": "Name may contain only letters, spaces, hyphens, and apostrophes."
      }
    },

    "email": {
      "type": "string",
      "minLength": 5,
      "maxLength": 254,
      "pattern": "^[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\\.[A-Za-z]{2,}$",
      "errorMessage": {
        "type": "Email must be a string.",
        "minLength": "Email must be at least 5 characters long.",
        "maxLength": "Email must not exceed 254 characters.",
        "pattern": "Email must be a valid email address (e.g., user@example.com)."
      }
    },

    "company": {
      "type": "string",
      "minLength": 2,
      "maxLength": 150,
      "pattern": "^[A-Za-z0-9À-ž&().,'\\-\\s]+$",
      "errorMessage": {
        "type": "Company name must be a string.",
        "minLength": "Company name must contain at least 2 characters.",
        "maxLength": "Company name must not exceed 150 characters.",
        "pattern": "Company name contains invalid characters."
      }
    },

    "purpose": {
      "type": "string",
      "minLength": 5,
      "maxLength": 300,
      "errorMessage": {
        "type": "Purpose must be a string.",
        "minLength": "Purpose must contain at least 5 characters.",
        "maxLength": "Purpose must not exceed 300 characters."
      }
    }
  },

  "errorMessage": {
    "required": {
      "name": "Name is required.",
      "email": "Email is required.",
      "company": "Company is required.",
      "purpose": "Purpose of visit is required."
    },
    "additionalProperties": "Unexpected additional fields are not allowed."
  }
}
