{
  "$id": "CredentialsSchema",
  "title": "Credentials schema",
  "type": "object",
  "required": [
    "url",
    "username",
    "password",
    "origin",
    "email"
  ],
  "properties": {
    "accountName": {
      "type": "string",
      "minLength": 1,
      "maxLength": 250,
      "errorMessage": {
        "type": "The 'credentials.accountName' property must be a string.",
        "minLength": "The 'credentials.accountName' property must not be empty.",
        "maxLength": "The 'credentials.accountName' property must not exceed 250 characters."
      }
    },
    "url": {
      "type": "string",
      "minLength": 1,
      "maxLength": 500,
      "errorMessage": {
        "type": "The 'credentials.url' property must be a string.",
        "minLength": "The 'credentials.url' property must not be empty.",
        "maxLength": "The 'credentials.url' property must not exceed 500 characters."
      }
    },
    "username": {
      "type": "string",
      "minLength": 1,
      "maxLength": 200,
      "errorMessage": {
        "type": "The 'credentials.username' property must be a string.",
        "minLength": "The 'credentials.username' property must not be empty.",
        "maxLength": "The 'credentials.username' property must not exceed 200 characters."
      }
    },
    "password": {
      "type": "string",
      "minLength": 1,
      "maxLength": 200,
      "errorMessage": {
        "type": "The 'credentials.password' property must be a string.",
        "minLength": "The 'credentials.password' property must not be empty.",
        "maxLength": "The 'credentials.password' property must not exceed 200 characters."
      }
    },
    "origin": {
      "type": "string",
      "minLength": 1,
      "maxLength": 500,
      "errorMessage": {
        "type": "The 'credentials.origin' property must be a string.",
        "minLength": "The 'credentials.origin' property must not be empty.",
        "maxLength": "The 'credentials.origin' property must not exceed 500 characters."
      }
    },
    "email": {
      "type": "string",
      "minLength": 1,
      "maxLength": 100,
      "pattern": "^\\S+@\\S+\\.\\S+$",
      "errorMessage": {
        "type": "The 'credentials.email' property must be a string.",
        "minLength": "The 'credentials.email' property must not be empty.",
        "maxLength": "The 'credentials.email' property must not exceed 100 characters.",
        "pattern": "The 'credentials.email' property must be a valid email format"
      }
    }
  },
  "errorMessage": {
    "required": {
      "url": "The 'credentials.url' property is required.",
      "username": "The 'credentials.username' property is required.",
      "password": "The 'credentials.password' property is required.",
      "origin": "The 'credentials.origin' property is required.",
      "email": "The 'credentials.email' property is required."
    },
    "type": "The credentials body must be an object."
  }
}
