/**
 * Represents a plain JavaScript object with string keys and string values.
 * Used as a decoy to indicate that a function or method accepts any object as input.
 *
 * @interface
 */
export interface CommonObject {
  [key: string]: any;
}
